# Frontend env (Vite — only VITE_* vars reach the browser; never put secrets here).

# LoanRegistry — the seam (deploy via contracts/script/Deploy.s.sol, then paste the address).
VITE_LOAN_REGISTRY_ADDRESS=

# LoanVault on Arc (Stage 2). Used for the borrower's claim() call.
VITE_LOAN_VAULT_ADDRESS=

# IncomeRouter on Arc (Stage 2/7) — the mock payout "cash out" button.
VITE_INCOME_ROUTER_ADDRESS=

# TranchePool on Arc (Stage 6/8) — the lender "Earn" deposit surface.
VITE_TRANCHE_POOL_ADDRESS=

# PassportRegistry on Arc (Stage 4 — World ID gate).
VITE_PASSPORT_REGISTRY_ADDRESS=

# World ID (docs/05). app_id from the World developer portal; action must match the deployed
# PassportRegistry's actionId (e.g. "mint-credit-passport").
VITE_WORLD_APP_ID=
VITE_WORLD_ACTION_ID=mint-credit-passport
# World ID 4.0 RP id (managed RP). Actions + verification live under this (server-side too).
VITE_WORLD_RP_ID=
# Optional eligibility gate: require the human to also prove an age via a World ID 4.0 document
# credential (privately — no birthdate revealed). Leave blank/0 for uniqueness-only. Set to e.g. 18
# to require 18+ — this needs a DOCUMENT-verified World App (passport/eID), not just proof-of-human.
VITE_WORLD_MIN_AGE=

# ERC-20 USDC on Arc (6 decimals) — look up via the Circle MCP server / address page
# (https://developers.circle.com/stablecoins/usdc-contract-addresses). NEVER hardcode (Golden Rule #5).
VITE_USDC_ADDRESS=

# Optional RPC override; otherwise viem's built-in Arc Testnet RPC is used.
VITE_ARC_RPC_URL=
